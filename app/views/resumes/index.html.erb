

<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
   <% @resumes.each do |resume| %>
      <div class="col">
         <div class="card shadow-sm">      
            <%= image_tag(resume.image.thumb.url, alt: 'Image') if resume.image?  %>
            <div class="card-body">
               <h3><%= resume.name %></h3>
               

               <p class="zagolovok"><%= resume.desc%></p>

               <p>Страниц: <%= resume.page %></p>

                <p><%= link_to "Download ", resume.attachment_url, class: "btn btn-warning"%></p>
               <%= button_to "Delete",  resume, method: :delete, class: "btn btn-warning", confirm: "Are you sure that you wish to delete #{resume.name}?" %>

            </div>
         </div>
            
      </div>

   <% end %>
</div>
